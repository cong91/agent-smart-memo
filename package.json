{
  "name": "agent-memo",
  "version": "3.0.0",
  "description": "Slot Memory Plugin for OpenClaw — structured key-value state management with auto-recall injection",
  "type": "module",
  "main": "dist/index.js",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-sqlite tests/test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.34.0",
    "openclaw": "*",
    "typescript": "^5.0.0"
  }
}
